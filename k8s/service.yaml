apiVersion: v1
kind: Service
metadata:
  name: agri-triage-service
spec:
  type: NodePort
  selector:
    app: agri-triage
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000
